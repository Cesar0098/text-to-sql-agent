# Dependencias del núcleo del agente (agent_core/, prompts/, data_generation/).
# streamlit se agrega en el paso siguiente, cuando se arme app.py.

anthropic==1.5.0
sqlalchemy==2.0.53
pandas==3.0.2
tabulate==0.10.0
faker==40.39.0
python-dotenv==1.2.2

# Si conectás el agente a otra base en lugar de la demo SQLite, sumá
# también el driver correspondiente (no vienen incluidos porque el
# proyecto funciona sin ellos con la base de demo):
#   psycopg2-binary   # para DATABASE_URL=postgresql://...
#   pymysql           # para DATABASE_URL=mysql+pymysql://...
